package org.sceylan.util.string;

import org.junit.Assert;
import org.junit.Test;
import org.sceylan.util.number.NumberUtil;

public class NumberUtilIsPrimeTest {
    /*@Test
    public void giveInteger_whenPrime_thenReturnTrue()
    {
        var input = 16;
        var expected =  true;
        Assert.assertTrue(String.format("%d must be prime",input),org.csystem.util.numeric.NumberUtil.isPrime(input));
    }
    @Test
    public void giveIntegerValue_whenNotPrime_thenReturnTrue()
    {
        var input = 16;
        var expected =  false;
        Assert.assertFalse( org.csystem.util.numeric.NumberUtil.isPrime(input));
    }*/
}
